//Friend Trinity Dickinson
//CIS 36B-61Y
//Professor Singh
//Assignment 2, Part 2
//Due: 10/20/2017

import java.lang.*;
import java.util.*;

public class Simulation
{
	private int heads, hperc, tails, tperc;
	public void simulate(Coin c, int times)
	{
		heads = 0;
		tails = 0;
		for (int i = 0; i < times; i++) {
			c.toss();
			System.out.printf("Side Up: %s%n", c.sideUp);
			if (c.sideUp.equals("heads")) { heads++; }
			if (c.sideUp.equals("tails")) { tails++; }
		}
		hperc = heads/times;
		tperc = tails/times;
		System.out.printf("Number of Heads: %d (%f\%) Number of Tails: %d (%f\%)%n", heads, hperc, tails, tperc);
	}
}